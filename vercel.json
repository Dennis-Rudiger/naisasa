{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["fra1"],
  "env": {
    "NEXTAUTH_URL": "https://naisasa.vercel.app",
    "PRISMA_CLIENT_ENGINE_TYPE": "dataproxy"
  }
}
